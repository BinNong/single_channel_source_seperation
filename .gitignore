# ---- Private: credentials & local tooling (do NOT publish) ----
tools/
migration_export/
.claude/
.playwright-cli/
AGENTS.md
CLAUDE.md

# ---- Companion papers (released separately) ----
# Paper 3 released 2026-08-24 (PHYCOM submission): paper3_open_set/ is now tracked.
paper2/
paper2_dp_mamba/
RESEARCH_ROADMAP.md
results/abl_no_bottleneck/
results/abl_no_crossgate*/
results/abl_no_dualpath*/
results/paper2_dp_mamba_phase1/

# ---- Third-party reference PDFs (copyright) ----
references/

# ---- Large artifacts: checkpoints & TensorBoard (released upon publication) ----
*.pt
runs/
checkpoints*/

# ---- Python / OS noise ----
__pycache__/
*.pyc
.DS_Store

# ---- LaTeX build artifacts ----
*.aux
*.log
*.out
*.blg
*.synctex.gz

# local verification venv (CPU torch, not part of the repo)
.venv_verify/
